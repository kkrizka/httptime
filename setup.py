#!/usr/bin/env python

import setuptools

setuptools.setup(
    name='httptime',
    version='0.1',
    description='Module for timing http queries inside a python program.',
    packages=[
        'httptime'
        ]
)
